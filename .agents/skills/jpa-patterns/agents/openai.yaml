interface:
  display_name: "JPA/Hibernate Patterns"
  short_description: "JPA/Hibernate patterns for entity design, relationships, query optimization, transactions, auditing, indexing, pagination, and pooling in Spring Boot."
  brand_color: "#6366F1"
  default_prompt: "Use JPA/Hibernate Patterns: JPA/Hibernate patterns for entity design, relationships, query optimization, transactions, auditing, indexing, pagination, and pooling in Spring Boot."
policy:
  allow_implicit_invocation: true
