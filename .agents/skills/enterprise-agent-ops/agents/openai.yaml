interface:
  display_name: "Enterprise Agent Ops"
  short_description: "Operate long-lived agent workloads with observability, security boundaries, and lifecycle management."
  brand_color: "#6366F1"
  default_prompt: "Use Enterprise Agent Ops: Operate long-lived agent workloads with observability, security boundaries, and lifecycle management."
policy:
  allow_implicit_invocation: true
